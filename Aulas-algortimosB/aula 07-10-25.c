/*Matrizes
#Alocação dinâmica de Memória
#Matrizes alocadas dinamicamente
As Matrizes alocadas dinamicamente usando ponteiros para ponteiros 
=====================================================
Um elemento da matriz pode ser referenciado de duas formas:
#por indexação: a[0][2] ou por ponteiros: *(a+2) = Isto porque uma matriz é alocada na memória de forma contígua

passagem por referencia e por valor 
    
*/

