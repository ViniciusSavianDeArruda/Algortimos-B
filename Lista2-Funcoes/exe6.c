//6.Desenvolva um programa, com funções para calcular a área e o perímetro de um terreno
//retangular. As medidas do terreno devem ser lidas. Por fim, os valores calculados devem ser informados ao usuário.

/*#include <stdio.h>

float CalculoArea(float comprimento ,float largura){

    return comprimento * largura;

}

float CalculoPerimetro(float comprimento, float largura){

    return 2 * (comprimento + largura); 

}

int main(){

    float comprimento;
    float largura;

    printf("Digite o comprimento do perimetro:");
    scanf("%f", &comprimento);

    printf("Digite a largura do perimetro:");
    scanf("%f", &largura);
    
    printf("A area do terreno é: %.2f",CalculoArea(comprimento, largura));

    printf("O perimetro do terreno é: %.2f",CalculoPerimetro(comprimento, largura));
    
    return 0;
}*/


