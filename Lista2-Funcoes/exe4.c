//4.Escrever um programa que lê uma string e chama uma função para escrevê-la verticalmente na tela.
/*#include <stdio.h>

void escreverVerticalmente(char *str) {
    int i = 0;
    while (str[i] != '\0') {
        printf("%c\n", str[i]);
        i++;
    }
}

int main() {
    char texto[100];

    printf("Digite uma string: ");
    fgets(texto, sizeof(texto), stdin);

    escreverVerticalmente(texto);

    return 0;
}*/
